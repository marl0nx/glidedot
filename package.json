{
  "name": "glidedot",
  "private": true,
  "scripts": {
    "build": "bun --filter='*' run build",
    "dev": "docker-compose up --build",
    "lint": "bun --filter='*' run lint"
  },
  "devDependencies": {
    "typescript": "latest"
  },
  "workspaces": [
    "apps/*"
  ],
  "dependencies": {
    "@types/node": "^26.0.1"
  },
  "overrides": {
    "unicorn-magic": "^0.4.0"
  }
}
